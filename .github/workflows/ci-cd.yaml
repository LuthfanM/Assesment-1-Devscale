name:   "CI/CD Pipeline"

on:
  pull_request:
    branches:
      - main

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Check Jobs
        run: echo 'Running CI/CD Pipeline for Pull Request to main branch'